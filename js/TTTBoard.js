angular
	.module('TTTApp')
	.factory('TTTBoard', TTTBoard);

	// setting number of tiles for Game Board
	function TTTBoard(){

//loc codez 
		// $scope.reset = function() {
  //       $scope.board = new Array(9);
  //       turn = 0;

  		var gameBoard = function(spaces){
  			this.spaces = spaces;
  			
  		}

	}